# Compose Video Project

This node waits for the project JSON and Demo UI branches, validates their slug, run directory, item addresses, and HTML files, then injects the references into the chapter JSON. It is the only node that associates Demo UI assets with hydrated video items.

